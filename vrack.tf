# Uncomment the below code to associate the project with the vRack if you have not already done so.
# 
# resource "ovh_vrack_cloudproject" "vrack" {
#   service_name = var.vrack_id
#   project_id   = var.project_id
# }